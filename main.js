/*src="https://cdn.jsdelivr.net/npm/js-confetti@latest/dist/js-confetti.browser.js"
const jsConfetti = new JSConfetti();

      function confettiFY() {
         jsConfetti.addConfetti({
            emojis: ['👍', '😊', '😁'],
            confettiRadius: 6,
            confettiNumber: 500,
         });
      }
      document.querySelector(".secret").addEventListener("click", confettiFY);
*/